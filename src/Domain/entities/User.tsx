import { Address } from "./Address";

export interface User {
    name:            string;
    lastname:        string;
    phone:           string;
    email:           string;
    password:        string;
}